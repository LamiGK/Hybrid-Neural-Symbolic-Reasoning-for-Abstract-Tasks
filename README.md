# Hybrid-Neural-Symbolic-Reasoning-for-Abstract-Tasks
The algorithm now integrates all four components (DSL, search, verification, perception) while handling the exact ARC competition format. You can run it on the 240-task dataset and generate submission files directly. I've created an interactive ARC Solver interface that properly generates and downloads a submission.json file. 
